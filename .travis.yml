language: c

before_script:
  - wget http://newyork.download.maverickservers.com/source/sourcemod-1.5.0-linux.tar.gz
  - tar zxvf sourcemod-1.5.0-linux.tar.gz
  - ls
  - cp addons/sourcemod/scripting/spcomp .
  - cp addons/sourcemod/scripting/compile.sh .
  - chmod +x spcomp
  - mkdir compiled
  - ls

script: "./compile.sh test-plugin.sp"